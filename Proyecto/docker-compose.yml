services:
  sqlserver:
    container_name: ventafacil-db
    hostname: mssql-db
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: 'Y'
      MSSQL_SA_PASSWORD: 'VentaFacilDb123!'
      MSSQL_DATA_DIR: /var/opt/mssql/data
      MSSQL_PID: 'Developer'
      MSSQL_TCP_PORT: 1433 
    ports:
      - "1455:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
      - ./log:/var/opt/mssql/log
      - ./secrets:/var/opt/mssql/secrets
      - ./init:/app/init
    networks:
      default:
        aliases:
          - sqlserver
  
   

  ventafacil.web:
    image: ventafacil.web
    build:
      context: .
      dockerfile: VentaFacil.web/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=VentaFacilDB;User Id=sa;Password=VentaFacilDb123!;TrustServerCertificate=true;
      - ConnectionStrings__MasterConnection=Server=sqlserver,1433;Database=master;User Id=sa;Password=VentaFacilDb123!;TrustServerCertificate=true;
      - ASPNETCORE_ENVIRONMENT=Development
      - DOTNET_RUNNING_IN_CONTAINER=true
      - ASPNETCORE_URLS=http://+:8080
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - shared_dataprotection_keys:/app/keys
      - app_uploads:/wwwroot/uploads
    depends_on:
      - sqlserver
    restart: unless-stopped

volumes:
  sqlserver_data:
  shared_dataprotection_keys:
    driver: local
    driver_opts:
      type: none
      device: ./dataprotection_keys
      o: bind
  app_uploads:
    driver: local
    driver_opts:
      type: none
      device: ./uploads
      o: bind
  
networks:
  default:
    driver: bridge